export interface ISession {
    id: string;
    expires?: Date;
    ipAddress?: string;
    userAgent?: string;
    createdAt?: Date;
    current?: boolean;
}
export interface SessionsState {
    loading: boolean;
    error?: any;
    currentSession?: ISession;
    sessions: ISession[];
}
