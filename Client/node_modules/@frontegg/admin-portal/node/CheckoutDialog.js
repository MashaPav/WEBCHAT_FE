'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var AppHolder = require('./AppHolder.js');

class CheckoutDialog {
    static show(opts, name = 'default') {
        AppHolder.AppHolder.getInstance(name).showCheckoutDialog(opts);
    }
    static hide(name = 'default') {
        AppHolder.AppHolder.getInstance(name).hideCheckoutDialog();
    }
}

exports.CheckoutDialog = CheckoutDialog;
