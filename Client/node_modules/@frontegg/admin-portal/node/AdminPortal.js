'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var AppHolder = require('./AppHolder.js');

class AdminPortal {
    static show(name = 'default') {
        AppHolder.AppHolder.getInstance(name).showAdminPortal();
    }
    static hide(name = 'default') {
        AppHolder.AppHolder.getInstance(name).hideAdminPortal();
    }
}

exports.AdminPortal = AdminPortal;
